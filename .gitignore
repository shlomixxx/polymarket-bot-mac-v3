node_modules
dist
logs
__pycache__
*.pyc
.DS_Store
.env
secrets*
tips_v2_cache.json

# מצב דמו (יתרה, פוזיציות, היסטוריית equity) — מתעדכן כל הזמן; לא קוד
**/demo_state.json

# SQLite journal files (transient; נוצרים בזמן ריצה ולא לקומיט)
*.db-shm
*.db-wal

# מסד הביקורת (audit.db) — runtime store, נוצר ע"י audit_tracker/בדיקות; לא לקומיט
engine/audit.db

# מסד התקלות (faults.db) — runtime store שמגיש המנוע החי (לשונית "תקלות"); נוצר ע"י
# fault_tracker/בדיקות ומשוחזר בזמן-ריצה; לא לקומיט. וגם כל sidecar faults.db של בדיקות.
engine/faults.db
**/faults.db

# Binance cockpit — sidecar audit ledger (runtime) ומאגר המפתחות fallback.
# המפתחות לעולם לא נכנסים ל-git: גם keyring וגם קובץ fallback בהרשאות 600.
**/binance_audit.db
**/.binance-futures-bot_pk
**/.polymarket_pk
*_pk

# גיבויי-נתונים מקומיים (off-Railway) — לא לקומיט (גדול/פרטי)
backups/

# playwright MCP screenshot/snapshot artifacts
.playwright-mcp/
/history.db
.claude/scheduled_tasks.lock
